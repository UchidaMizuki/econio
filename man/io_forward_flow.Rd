% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forward-flow.R
\name{io_forward_flow}
\alias{io_forward_flow}
\title{Forward flow of a production network}
\usage{
io_forward_flow(data, order)
}
\arguments{
\item{data}{An \code{econ_io_table} object.}

\item{order}{A \code{dibble} with a single \code{industry} dimension and one numeric
value per industry, such as the output of \code{\link[=io_trophic_level]{io_trophic_level()}},
\code{\link[=io_upstreamness]{io_upstreamness()}} or \code{\link[=io_downstreamness]{io_downstreamness()}}.}
}
\value{
A \code{dibble} of domestic intermediate transactions with dimensions
\code{c("input", "output")}, with backward and same-level transactions set to
\code{0}.
}
\description{
Given a per-industry ordering (e.g. from \code{\link[=io_trophic_level]{io_trophic_level()}}), zero out
every domestic intermediate transaction that does not run strictly from a
lower-ordered (more upstream) industry to a higher-ordered (more
downstream) one.
}
\details{
The table is first converted to a noncompetitive import type table with
\code{\link[=io_table_to_noncompetitive_import]{io_table_to_noncompetitive_import()}}, as in \code{\link[=io_trophic_level]{io_trophic_level()}}, so that
the industry-by-industry block \code{w[i, j]} holds purely domestic intermediate
transactions. An entry \code{w[i, j]} is kept only if \code{order[i] < order[j]}; it
is zeroed otherwise, which also removes self-loops (\code{i == j}) and
transactions between industries at the same level.

\code{io_forward_flow()} does \strong{not} search for an ordering that minimizes the
zeroed-out (backward) flow; it only applies the ordering supplied in
\code{order}. Finding an optimal ordering is a combinatorial optimization (see
Simpson and Tsukui, 1965) that this function does not perform.
}
\examples{
\dontrun{
io_forward_flow(iotable, order = io_trophic_level(iotable))
}

}
\references{
Simpson, D. and Tsukui, J. (1965). The fundamental structure of
input-output tables: An international comparison. \emph{The Review of Economics
and Statistics}, 47(4), 434-446.
}
