% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/streamness.R
\name{io_streamness_position}
\alias{io_streamness_position}
\title{Supply chain position}
\usage{
io_streamness_position(
  data,
  open_economy = NULL,
  normalize = FALSE,
  type = c("difference", "relative", "log_ratio")
)
}
\arguments{
\item{data}{An \code{econ_io_table} object.}

\item{open_economy}{A scalar logical passed to \code{\link[=io_leontief_inverse]{io_leontief_inverse()}} /
\code{\link[=io_ghosh_inverse]{io_ghosh_inverse()}}. If \code{TRUE}, open economy assumptions are used.}

\item{normalize}{A scalar logical. If \code{TRUE}, the measure is rescaled to
have a mean of 1 across sectors. By default, \code{FALSE}.}

\item{type}{One of \code{"difference"}, \code{"relative"}, or \code{"log_ratio"}. See
Details.}
}
\value{
An \code{econ_io_table} object with one value per industry.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Net position of each industry within the supply chain, derived from
\code{io_upstreamness()} (\code{U}) and \code{io_downstreamness()} (\code{D}). A positive
value indicates that the industry sits relatively further upstream
(closer to primary inputs); a negative value indicates it sits relatively
further downstream (closer to final demand).

\code{type} selects the combination:
\itemize{
\item \code{"difference"} (default): \code{U - D}.
\item \code{"relative"}: \code{(U - D) / (U + D)}, bounded in \verb{[-1, 1]}.
\item \code{"log_ratio"}: \code{log(U / D)}, scale-robust.
}

Unlike \code{\link[=io_streamness_length]{io_streamness_length()}}, this is not a single named index from a
specific paper; these are natural arithmetic complements to
\code{io_upstreamness()} and \code{io_downstreamness()}, provided as an experimental
convenience. The exact formulation(s) may change in future releases.

When \code{normalize = TRUE}, \code{U} and \code{D} are each rescaled to a mean of 1
before combining, so \code{type = "difference"} has a mean of 0 across
industries: a relative-position measure centered on the average.
}
\examples{
\dontrun{
io_streamness_position(iotable)
io_streamness_position(iotable, normalize = TRUE) # mean 0 across industries
io_streamness_position(iotable, type = "relative")
io_streamness_position(iotable, type = "log_ratio")
}

}
\seealso{
\code{\link[=io_streamness_length]{io_streamness_length()}}
}
