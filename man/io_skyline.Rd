% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{io_skyline}
\alias{io_skyline}
\alias{tidy.econ_io_table}
\alias{autolayer.econ_io_table}
\alias{autoplot.econ_io_table}
\title{Tidy, layer, and plot an input-output table}
\usage{
\method{tidy}{econ_io_table}(x, type, ...)

\method{autolayer}{econ_io_table}(object, type, ...)

\method{autoplot}{econ_io_table}(object, type, ...)
}
\arguments{
\item{x, object}{An \code{econ_io_table} object.}

\item{type}{A scalar character. The type of summary. Currently only
\code{"skyline"} is supported.}

\item{...}{Additional arguments passed to the underlying summary. For
\code{autolayer()}, this includes \code{geom} (\code{"rect"} (default) or \code{"segment"}) and
any further arguments passed to the underlying geometry.}
}
\value{
\itemize{
\item \code{tidy()}: a \code{\link[tibble:tibble]{tibble::tibble()}}.
\item \code{autolayer()}: a list of \pkg{ggplot2} layers.
\item \code{autoplot()}: a \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
}
\description{
Methods for \code{\link[generics:tidy]{generics::tidy()}}, \code{\link[ggplot2:autolayer]{ggplot2::autolayer()}}, and
\code{\link[ggplot2:autoplot]{ggplot2::autoplot()}} that summarise an \code{econ_io_table} for visualization.
}
\details{
Currently only \code{type = "skyline"} is supported, which produces a \emph{skyline
chart} (a.k.a. an industry-by-industry diagram of production inducement).
For each industry the chart shows, as a stacked bar:
\itemize{
\item the width: the total production induced by final demand and exports, and
\item the height: the self-sufficiency rate and the import rate, expressed as a
share of domestic final demand.
}

The skyline chart is only defined for competitive import type tables
(created with \code{\link[=io_table_to_competitive_import]{io_table_to_competitive_import()}} or
\code{\link[=io_table_regional]{io_table_regional()}}/\code{\link[=io_table_multiregional]{io_table_multiregional()}} with
\code{competitive_import = TRUE}).
\itemize{
\item \code{tidy()} returns the underlying data in a tidy \code{\link[tibble:tibble]{tibble::tibble()}} (one row
per industry and rate type), suitable for custom plots.
\item \code{autolayer()} returns \pkg{ggplot2} layers to add to an existing plot.
Pass \code{geom = "rect"} (default) for the skyline bars or \code{geom = "segment"}
for the self-sufficiency outline.
\item \code{autoplot()} returns a complete \pkg{ggplot2} skyline chart.
}
}
\examples{
\dontrun{
# `iotable` is a competitive import type `econ_io_table`.
library(ggplot2)

# Tidy data for a custom plot.
tidy(iotable, type = "skyline")

# A complete skyline chart.
autoplot(iotable, type = "skyline")

# Or build one up from layers.
ggplot() +
  autolayer(iotable, type = "skyline", geom = "rect")
}

}
