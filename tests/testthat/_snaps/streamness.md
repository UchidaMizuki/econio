# normalize must be a scalar logical

    Code
      io_downstreamness(iotable, normalize = "yes")
    Condition
      Error in `io_downstreamness()`:
      ! `normalize` must be `TRUE` or `FALSE`, not the string "yes".

# io_streamness_length normalize must be a scalar logical

    Code
      io_streamness_length(iotable, normalize = "yes")
    Condition
      Error in `io_streamness_length()`:
      ! `normalize` must be `TRUE` or `FALSE`, not the string "yes".

# io_streamness_position normalize must be a scalar logical

    Code
      io_streamness_position(iotable, normalize = "yes")
    Condition
      Error in `io_streamness_position()`:
      ! `normalize` must be `TRUE` or `FALSE`, not the string "yes".

# io_streamness_position type must be one of the allowed values

    Code
      io_streamness_position(iotable, type = "invalid")
    Condition
      Error in `io_streamness_position()`:
      ! `type` must be one of "difference", "relative", or "log_ratio", not "invalid".

