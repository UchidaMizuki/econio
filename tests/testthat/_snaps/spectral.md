# io_spectral_embedding validates dims

    Code
      io_spectral_embedding(iotable, dims = 0)
    Condition
      Error in `io_spectral_embedding()`:
      ! `dims` must be a scalar integer between 1 and 2.

